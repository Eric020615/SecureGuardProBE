export interface CreateNoticeDto {
    title: string;
    description: string;
    startDate: string;
    endDate: string;
}

export interface UpdateNoticeDto {
    title: string;
    description: string;
    startDate: string;
    endDate: string;
}